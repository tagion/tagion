name: Build D code docmentation
on:
  workflow_dispatch:
  workflow_call:
    secrets:
      GITBOT_TOKEN:
        required: true

env:
  GH_TOKEN: ${{ secrets.GITBOT_TOKEN }}

jobs:
  ddoc:
    runs-on: ubuntu-latest
    steps:

      - uses: dlang-community/setup-dlang@v1
        with:
          compiler: dmd-latest

      - uses: actions/checkout@v4
        with: 
          path: ./tagion

      - name: Build docmentation
        run: |
          cd tagion
          make ddoc

      - name: update ddoc repo
        run: |
          git clone https://${{ secrets.GITBOT_TOKEN }}@github.com/tagion/ddoc.git master
          cp -r ./tagion/build/ddoc/ ddoc/
          cd ddoc
          git config user.email "gitbot@decard.io"
          git config user.name "gitbot"
          git add .
          git commit -m "ddocs updated" || echo "nothing to commit"
          git push https://${{ secrets.GITBOT_TOKEN }}@github.com/tagion/ddoc.git master
