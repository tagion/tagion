name: Acceptance tests
on:
  workflow_run:
    workflows: ["Commit Build and Test"]
    branches: [current-commit]
    types:
      - completed

jobs:
  run_acceptance_stage_tests:
    runs-on: self-hosted
    steps:
      - name: Mount Artifact
        run: | 
          mkdir -p ARTIFACT
          archivemount ../my_balls.tar.gz ARTIFACT
      - name: Testing
        run: echo $(ls ARTIFACT)

      
      - name: Unmount Artifact
        if: failure() || success()
        run: |
          fusermount -u ARTIFACT
      



      

      # Additional steps here

