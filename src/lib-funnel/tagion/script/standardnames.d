module tagion.script.standardnames;

import tagion.communication.HiRPC;

@safe:
enum StdNames {
    owner = "$Y",
    value = "$V",
    time = "$t",
    nonce = "$x",
    values = "$vals",
    derive = "$D",
    epoch = "#$epoch",
    bullseye = "$eye",
    name = "#$name",
    previous = "$prev",
    tagion = "#tagion",
    nodekey = "#$node",
}
